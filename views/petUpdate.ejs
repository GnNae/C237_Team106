<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>The Pet Shop</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">The Pet Shop</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addPets">Add New Pet</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Pets</h2></div>
        <br>
        <div class="text-center">
          <% if (pet) { %> 
            <form action="/updatePet/id method="POST" enctype="multipart/form-data">
                <label for="name">Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= pet.name %>" required><br><br>

                <label for="quantity">Age:</label><br>
                <input type="number" id="Age" name="Age" min="0" step="1" value=<%= pet.age %> required><br><br>

                <label for="price">Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value=<%= pet.price %> required><br><br>

                <label for="breed">Breed:</label><br>
                <input type="text" id="breed" name="breed" value=<%= pet.breed %> required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value=<%= pet.image %> readonly><br>
                <img src = "/images/<%= pet.image %>" width="20%"><br><br>

                New Image:<br><input type= "file" id="image" name="image" accept="image/*" ><br><br>  // remove value=<%=pet.image %> 

                <button type="submit">Update Pet</button>
            </form>
          <% } else { %>
              <p>No Pet found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
