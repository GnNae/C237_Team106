<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>The Pet Shop</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">The Pet Shop</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <% if (user.role === "admin") { %>
                <li class="nav-item">
                  <a class="nav-link" href="/inventory">Inventory</a> <!-- Matches app.js /inventory -->
                </li> 
                <li class="nav-item">
                  <a class="nav-link" href="/addPet">Add New Pet</a>
                </li> 
              <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <p>Welcome, <%= user.username %> (<%= user.role %>)</p>
        <br>
        <div class="text-center"><h2>Pet Details</h2></div>
        <br>
        <% if (pet) { %> <!-- Check if pet data exists -->
            <div>
                <p><b>Breed:</b><br> <%= pet.breed %></p>
                <p><b>Image:</b><br> <img src = "<%= pet.image %>" width="20%">" width="20%"></p> // change img src
                <p><b>Name:</b><br> <%= pet.name %></p> //change to pet.name
                <p><b>Age:</b><br> <%= pet.age %></p>
                <p><b>Gender:</b><br> <%= pet.gender %></p>
                <p><b>Price:</b><br> $<%= pet.price.toFixed(2) %></p> 
            </div>
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        <% if (user.role === "admin") { %>
          <a href ="/inventory">Back</a> <!-- Matches app.js /inventory -->
        <% } else { %>
          <a href ="/shopping">Back</a> <!-- Matches app.js /shopping -->
        <% } %>
    </div>
    
</body>
</html>
